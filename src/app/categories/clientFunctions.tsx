'use client';

export default function CategoryList(catArr:any[]) {
    function LoadCat(category:any) {
        console.log('Category clicked:', category.category_name);
        if(category.category_id=='1'){
        }
    }
}

